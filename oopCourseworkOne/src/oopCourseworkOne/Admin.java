package oopCourseworkOne;
public class Admin extends User {

 Admin(int userID, String username, String name, Address userAddress, UserCategory userType) {
        super(userID, username, name, userAddress, userType);
    }
}